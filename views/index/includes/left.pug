div(class='col-4')
   div(class='border rounded p-2 m-auto p-4')
      h4 Roommates
      hr(class='bg-light w-25 mx-auto')
      table(class='table text-white')
         thead
            th Nombre 
            th Debe 
            th Recibe 
         tbody(id='roomates')
            each roommate in roommates
               tr
                  td= `${roommate.name} ${roommate.lastname}`
                  td(class='text-red')= roommate.debt === 0 ? '-' : roommate.debt
                  td(class='text-green')= roommate.receive === 0 ? '-' : roommate.receive
      form(action='/roommates' method='POST')
         button(class='btn text-white mb-2' type='submit' name='addRoommate') Agregar Random Roommate 