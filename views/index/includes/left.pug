.col-4
   .border.rounded.p-2.m-auto.p-4
      h4 Roommates
      hr.bg-light.w-25.mx-auto
      table.table.text-white
         thead
            th Nombre 
            th Debe 
            th Recibe 
         tbody#roommates
            each roommate in roommates
               tr
                  td= `${roommate.name} ${roommate.lastname}`
                  td.text-danger= roommate.debt === 0 ? '-' : roommate.debt
                  td.text-success= roommate.receive === 0 ? '-' : roommate.receive
      form(action='/roommates' method='POST')
         button.btn.text-white.mb-2(type='submit' name='addRoommate') Agregar Random Roommate